import { Result, ApiResult } from './../ApiResult';
class DataSource {
    // async safeApiCall(promise: Promise<ApiResult>): Promise<ApiResult> | Promise<Res> {
    //     try {
    //         const response = await promise
    //         return Result.fromAxiosResponse(response)
    //     } catch (error) {
    //         return Result.from
    //     }
    // }
}